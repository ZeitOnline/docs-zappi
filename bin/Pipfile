[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Keep in sync with ZeitOnline/friedbert-deployment,
# so our test/validation behaviour matches that of the runtime.
pyramid_openapi3 = "==0.9.0"
openapi_core = "==0.13.5"
openapi_schema_validator = "==0.1.1"
openapi_spec_validator = "==0.2.9"

[requires]
python_version = "3"
