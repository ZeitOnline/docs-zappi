[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Keep in sync with ZeitOnline/friedbert-deployment,
# so our test/validation behaviour matches that of the runtime.
pyramid_openapi3 = "==0.16.0"
openapi_core = "==0.16.6"
openapi_schema_validator = "==0.4.4"
openapi_spec_validator = "==0.5.7"

[requires]
python_version = "3"
